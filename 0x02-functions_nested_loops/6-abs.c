#include "main.h"
/**
 * _abs - function that computes the absolute value of int
 *
 * Description: The standard library provided a similst funct..
 *
 * Return: (0)
 *
 */
int _abs(int r)
{
	if (r >= 0)
	{
		return (r);
	}
	else
	{
		return (r * = -1);
	}
}
